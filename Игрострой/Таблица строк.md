---
tags:
  - note
  - gamedev
  - stringtable
---
### Проблема:

Представьте типичный код в разработке игры:

```cpp
void showGameOver() {
    drawText("Game Over!", 100, 100);  // А что если нужен русский?
    drawText("Press SPACE to restart", 100, 130);  // Или французский?
}
```

А теперь умножьте это на **1000+ строк текста** в игре, добавьте поддержку **5+ языков**, учтите необходимость **быстрых правок** текста без перекомпиляции... Получается кошмар поддержки!
### Что такое String Table?

**String Table** (таблица строк) - это централизованная база данных текстовых ресурсов игры, где каждый текстовый элемент идентифицируется уникальным ключом (ID), а не хранится напрямую в коде.

```cpp
void showGameOver() {
    drawText(stringTable.get("game_over_title"), 100, 100);
    drawText(stringTable.get("game_over_hint"), 100, 130);
}
```
### Преимущества такой системы

### 1. 🔤 Локализация (Multi-language support)

**Без String Table:**

```cpp
if (language == "ru") {
    title = "Конец игры!";
} else if (language == "fr") {
    title = "Jeu Terminé!";
} else {
    title = "Game Over!";
}
// А теперь добавьте испанский, китайский, арабский...
```

C **String Table:**

```cpp
{
  "game_over_title": {
    "en": "Game Over!",
    "ru": "Конец игры!",
    "fr": "Jeu Terminé!",
    "es": "¡Juego Terminado!",
    "zh": "游戏结束！"
  }
}
```

### 2. 🔄 Гибкость и лёгкость обновлений

**Ситуация:** Вам нужно изменить текст "Press START to begin" на "Press START button to begin game".

**Без String Table:**
- Ищете все вхождения в коде
- Рискуете пропустить некоторые
- Перекомпилируете всю игру

**С String Table:**
- Меняете одно значение в JSON-файле
- Запускаете игру
- Готово!
### 3. 👥 Коллаборация в команде

**Разделение ответственности:**
- **Программисты** работают с логикой: `stringTable.get("item_found")`
- **Геймдизайнеры** редактируют тексты в JSON без редактирования кода
- **Локализаторы** добавляют переводы без редактирования кода
- **Тестировщики** проверяют отображение без сборки игры

**Пример из реального проекта:**
📁 [StringTable Manager](projects/StringTableEditor/README.md) - полнофункциональный редактор таблиц строк на Qt/C++